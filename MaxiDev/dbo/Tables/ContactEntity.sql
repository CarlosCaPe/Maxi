CREATE TABLE [dbo].[ContactEntity] (
    [IdContactEntity] INT           NOT NULL,
    [Name]            VARCHAR (500) NULL,
    CONSTRAINT [PK_ContactEntity] PRIMARY KEY CLUSTERED ([IdContactEntity] ASC)
);

