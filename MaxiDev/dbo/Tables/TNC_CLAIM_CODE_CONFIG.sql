CREATE TABLE [dbo].[TNC_CLAIM_CODE_CONFIG] (
    [batch_support]    BIT          NOT NULL,
    [batch_id]         INT          NOT NULL,
    [transmitter_code] VARCHAR (20) NOT NULL,
    [current_number]   VARCHAR (50) NOT NULL
);

